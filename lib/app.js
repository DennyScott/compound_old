Router.map(function() {
  this.route('home', {
      path: '/'

    }),

    this.route('dashboard', {
      path: 'dashboard'
    }),

    this.route('profile', {
      path: 'profile'
    }),

    this.route('project', {
      path:'project'
    })
});
